apiVersion: v1
kind: Service
metadata:
  name: bank-app-service
spec:
  type: LoadBalancer
  selector:
    app: bank-app
  ports:
    - name: http
      protocol: TCP
      port: 3000
      targetPort: 80
